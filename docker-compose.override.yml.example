# Docker Compose Override
# The override file is ignored by git and will not be overwritten

services:
  bettershift:
    # Change the port mapping
    ports:
      - "3000:3000"

    # Connect to external proxy network
    # networks:
    #   - proxy-network

    # Add additional environment variables
    # environment:
    #   - NODE_ENV=production
    #   - CUSTOM_VAR=value

    # Mount additional volumes
    # volumes:
    #   - ./custom-config:/app/config

    # Override restart policy
    # restart: always

    # Add resource limits
    # deploy:
    #   resources:
    #     limits:
    #       cpus: '0.5'
    #       memory: 512M
# Network configuration
# networks:
#   proxy-network:
#     external: true
